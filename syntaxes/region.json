{
  "scopeName": "region",
  "injectionSelector": "L:comment.line.double-slash",
  "patterns": [{ "include": "#region" }],
  "repository": { "region": { "match": "#region", "name": "comment.region" } }
}
